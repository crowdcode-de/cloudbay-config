message: "hello tomorrow"
io:
  crowdcode:
    greeting:
      welcome-message: "Greetings From The Config Server! ERNEUT"
spring:
  boot:
    admin:
      client:
        url: http://localhost:9091
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    jmx:
      exposure:
        include: "*"
    web:
      exposure:
        include: "*"
