message: "hello tomorrow"
io:
  crowdcode:
    greeting:
      welcome-message: "Hallo Spring Boot vom Config Server Test"
spring:
  boot:
    admin:
      client:
        url: http://localhost:9091
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"
